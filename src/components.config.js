import Note from './components/Note'
import Reference from './components/Reference'
// import your components here

// Add custom components here (components keys must be in 
// lowercase - as in Markdown). See README.md for more details on how to
// use in markdown.
const config = {
  note: Note,
  ref: Reference,
}

export default config;
